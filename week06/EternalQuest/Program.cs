// For extra credit, I included a "times completed" to eternal goals listed, so that if the goal has daily habits, it keeps track of them.


using System;

class Program
{
    static void Main(string[] args)
    {
        GoalManager goalManager = new GoalManager();
        goalManager.Start();
    }
}